import { LSystemJSONParameters } from './lsystem-jsonparameters';

describe('LSystemJSONParameters', () => {
  it('should create an instance', () => {
    expect(new LSystemJSONParameters()).toBeTruthy();
  });
});
